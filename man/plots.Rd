% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{plots}
\alias{plots}
\title{Generate Posterior Predictive Check Plots}
\usage{
plots(draws, df_test)
}
\arguments{
\item{draws}{A stanfit object or list containing posterior predictions. Must contain:
\itemize{
\item gf_new (matrix) - Posterior predictions for home goals
\item ga_new (matrix) - Posterior predictions for away goals
}}

\item{df_test}{Test Data frame containing observed goal data. Must contain:
\itemize{
\item gf (numeric) - Observed home goals
\item ga (numeric) - Observed away goals
}}
}
\value{
A bayesplot grid object containing the combined plots
}
\description{
Creates diagnostic plots comparing observed goals with posterior predictions
from a Bayesian model. Produces two sets of plots:
\enumerate{
\item Goal distribution comparisons using bar plots
\item Mean goal comparisons using statistical plots
}
}
\examples{
\dontrun{
# Requires Stan model output
library(bayesplot)

# Sample test data
test_data <- data.frame(
  gf = c(2, 1, 3, 0, 2),
  ga = c(1, 1, 2, 2, 0)
)

# Sample posterior draws (typically from stanfit object)
sample_draws <- list(
  gf_new = matrix(sample(0:4, 500, replace = TRUE), 
  ga_new = matrix(sample(0:4, 500, replace = TRUE))
)

# Generate plots
plots(sample_draws, test_data)
}

}
